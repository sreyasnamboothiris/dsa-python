array = [8, 5, 9, 1, 6, 7]


def merge(arr, l, r, mid):
    left_array = []
    right_array = []
    n1 = mid - l + 1
    n2 = r - mid
    for i in range(n1):
        left_array[i]


def merge_sort(arr, l, r):
    if l < r:
        mid = (l + r) // 2


merge_sort(array, 0, len(array))
